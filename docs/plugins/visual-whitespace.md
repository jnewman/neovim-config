# visual-whitespace.nvim

## Purpose

Highlights whitespace characters (spaces, tabs) inside the active visual
selection, so exactly what whitespace is selected is visible before you
yank/delete/replace it.

## Keybindings

None — activates automatically while a visual selection is active.

## Config Notes

Default config (`require("visual-whitespace").setup()` with no options).
