
# Claude Code per-user local settings
.claude/settings.local.json

# Nix build output symlink
result

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.claude/settings.local.json

# these are mostly used locally and annoying to keep merging..
.beads/
