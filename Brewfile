brew "neovim"
brew "lua-language-server"
brew "stylua"
brew "gh"
brew "yaml-language-server"
brew "yq"
# mermaid preview (lua/config/mermaid.lua): mmdc + image.nvim's CLI processor
brew "mermaid-cli"
brew "imagemagick"
# nvim-treesitter (main) compiles parsers on install; grammars without a prebuilt
# parser.c (e.g. haskell) need the tree-sitter CLI for `tree-sitter generate`
brew "tree-sitter"
# jsonls: npm install -g vscode-langservers-extracted
